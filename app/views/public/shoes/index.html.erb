<div class="container my-5">
  <div class="row">
    <h4 class="col-5 font-weight-bold">
      シューズ一覧 (全<%= @shoes.count %>件)
    </h4>
    <div class="col-3 offset-3 text-right">
      <%=link_to "シューズ登録", new_shoe_path, class: "btn btn-sm btn-success" %>
    </div>
  </div>
  <div class="row my-3">
    <% @shoes.each do |shoe| %>
      <div class="col-3 d-flex flex-column bd-highlight mb-3">
        <%= link_to shoe_path(shoe.id) do %>
          <div class="p-2 bd-highlight">
            <%= image_tag shoe.shoeImage, size: "200x200" %>
          </div>
        <% end %>
        <div class="p-2 bd-highlight">
          <%= shoe.name %>
        </div>
      </div>
    <% end %>
  </div>
</div>
