<div class="container my-5">
  <div class="row">
    <div class="col-md-auto mb-3 bg-light">
      <h4>レビュー一覧</h4>
    </div>
  </div>

  <table class="table table-hover table-inverse">
    <thead>
      <tr>
        <th>会員ID</th>
        <th>アイテム画像</th>
        <th>レビュー</th>
      </tr>
    </thead>
      <tbody>
        <tr>
        <% @inners.each do |inner| %>
          <td><%= inner.user_id %></td>
          <td>
            <%= link_to inner_path(inner.id) do %>
              <%= image_tag inner.innerImage, size: "100x100" %>
            <% end %>
          </td>
          <td><%= inner.review %></td>
        <% end %>
        </tr>
      </tbody>
  </table>
</div>